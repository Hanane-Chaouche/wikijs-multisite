services:
  nginx:
    image: nginx:latest
    restart: always
    container_name: nginx
    ports:
      - "80:80"
    volumes:
      - ./wikijs.conf:/etc/nginx/conf.d/wikijs.conf
    network_mode: host
