version: "3"

services:
  nginx:
    image: nginx:latest
    container_name: nginx
    ports:
      - "80:80"  # Redirige les requêtes HTTP vers le conteneur Nginx
    volumes:
      - ./wikijs.conf:/etc/nginx/conf.d/default.conf  # Monte ta config Nginx

